# to create a django project app directory in docker container 
# and map it to app directory on host as configured in docker-compose.yml
# -- rm remove the container immediately after run...
docker-compose run --rm app sh -c "django-admin startproject app ."